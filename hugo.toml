baseURL = 'https://yrhius.github.io/'
defaultContentLanguage = "fr"
languageCode = 'fr-fr'

theme = 'gokarna'
title = 'Portfolio Samuel GOUBY'


[menu]
  [[menu.main]]
    # Display name
    name = "About Me"

    # Relative URL slug (appended to baseURL)
    url = "/aboutme/MaPresentation"

    # Lower weights are listed first in the menu (leftmost); higher weights are
    # listed last in the menu (rightmost)
    weight = 2

  [[menu.main]]
    # Display name
    name = "Posts"

    # Relative URL slug (appended to baseURL)
    url = "/posts/"

    # Lower weights are listed first in the menu (leftmost); higher weights are
    # listed last in the menu (rightmost)
    weight = 2

  [[menu.main]]
    name = "Tags"
    url = "/tags/"
    weight = 3
    
  [[menu.main]]
    # Unique identifiers are required for menu entries without a name property,
    # or for menu entries which re-use a name
    identifier = "github"

    # Absolute URL to external resource
    url = "https://github.com/Yrhius"
    weight = 4
    
    # Surround the menu entry (or name) with HTML content, such as Feather
    # icons: https://feathericons.com
    pre = "<span data-feather='github'></span>"
    post = ""

  [[menu.main]]
    # Unique identifiers are required for menu entries without a name property,
    # or for menu entries which re-use a name
    identifier = "linkedin"

    # Absolute URL to external resource
    url = "https://www.linkedin.com/in/samuel-gouby"
    weight = 5
    
    # Surround the menu entry (or name) with HTML content, such as Feather
    # icons: https://feathericons.com
    pre = "<span data-feather='linkedin'></span>"
    post = ""

  [[menu.main]]
    # Display name
    name = "Scolaire"

    # Relative URL slug (appended to baseURL)
    url = "/scolar/Apprentissage"

    # Lower weights are listed first in the menu (leftmost); higher weights are
    # listed last in the menu (rightmost)
    weight = 6


[params]
  accentColor = "#FF4D4D"
  avatarURL = "/images/pdp.png"
  avatarSize = "size-xl"
  avatarAltText = "ma photo de profil"
  dateFormat = "2 January, 2006"
  footer = "Samuel GOUBY"
  metaKeywords = ["portfolio", "informatique", "système", "réseau", "Samuel", "Gouby", "étudiant", "Montpellier"]
  showBackToTopButton = true
  togglePreviousAndNextButtons = true

    socialIcons = [
    {name = "github", url = "https://github.com/Yrhius"},
    {name = "linkedin", url = "https://www.linkedin.com/in/samuel-gouby"},
    #{name = "rootme", url = "https://www.root-me.org/Yrhius"},
  ]

  #highlight code
  pygmentsStyle = "aura-theme-dark"


[markup]
  [markup.tableOfContents]
    startLevel = 1
    endLevel = 3
    ordered = false


